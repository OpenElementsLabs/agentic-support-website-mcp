package com.openelements.content;

import org.springframework.context.annotation.Configuration;

/**
 * Central configuration for the content-specific part of the application.
 *
 * <p>This is an intentionally empty placeholder in the project skeleton (spec 001). Later specs fill
 * it in with {@code @Bean} definitions and component-scanned collaborators (content source
 * configuration, crawler, indexer, search service, MCP tools).
 */
@Configuration
public class ContentConfig {
}
